<h2>Liste des entreprises créées</h2>
<div class="row">
  <div class="panel-body">
    <% @entreprises.each do |entreprise| %>
      <div class="row">
        <div class="col-md-7">
          <h4><%= entreprise.name_entreprise %></h4>
        </div>
        <div class="col-md-3 right">
          <%= link_to "Update", edit_entreprise_path(entreprise), class: "btn btn-form" %>
        </div>
      </div>
    <% end %>
  </div>
</div>